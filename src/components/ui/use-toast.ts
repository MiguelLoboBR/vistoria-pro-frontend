
// Re-export from the hooks file to maintain backward compatibility
import { useToast, toast, type Toast, type ToastT, type ToastProps } from "@/hooks/use-toast";

export { useToast, toast, type Toast, type ToastT, type ToastProps };
